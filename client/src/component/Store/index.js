// import { createStore } from "redux";

// const INITIAL_VALUE = {
//   data: [],
// };

// const dataReducer = (store = INITIAL_VALUE, action) => {
//   if (action.type === "SET_DATA") {
//     return { data: [...store.data, ...action.payload] };
//   }
//   console.log(store);
//   return store;
// };

// const dataStore = createStore(dataReducer);

// export default dataStore;
